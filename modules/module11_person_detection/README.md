# Module 11 — Human Detection & Face Recognition (OpenCV)

### 🎯 Objective
Detect multiple humans, recognize known persons by folder, and identify unknowns.

---

## ⚙️ Setup
```bash
sudo apt install libopenblas-dev libjpeg-dev
pip install -r requirements.txt
